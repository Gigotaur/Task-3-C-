#include <iostream>
#include <vector>

using namespace std;


int main()
{
    std::vector <int> n = {1, 2, 3, 4, 5, 6};
    std::vector <int> fin = {};
    
   for (int i=0; i < n.size(); i++){
    if (i % 2 == 0){
        fin.push_back(n[i]);
    }
    cout << fin <<;
   }

    return 0;
}
